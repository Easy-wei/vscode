function polynomial() {
	var arry = Array.prototype.slice.call(arguments);
	var a;
	for (var i = 0; i < array.length, i++) {
		array[i] = array[i].signed()
	}
	for (var i = 0; i < array.length; i++) {
		a = array.join(`x^{` + (array.length - 1) + `}`)
	}
	var a =
		return array
}